

# This was inserted by `prisma init`:
# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="postgresql://USUARIO:CONTRASENA@localhost:5432/NOMBRE_BD?schema=public"
ENTORNO="desarrollo"
PORT=3000
URL_PUBLICA="http://localhost:3000"

# Autenticacion
JWT_SECRETO="cambia-esta-clave-por-un-secreto-largo-y-unico"
JWT_DURACION="7d"

RETENCION_HISTORIAL_DIAS=7
PORT=3000
ENTORNO="desarrollo"
URL_PUBLICA="http://localhost:3000"
# Izipay: completar con las credenciales entregadas por Izipay antes de activar cobros reales.
IZIPAY_AMBIENTE="sandbox"
IZIPAY_USUARIO=""
IZIPAY_CONTRASENA=""
IZIPAY_LLAVE_PUBLICA=""
IZIPAY_LLAVE_SECRETA=""
IZIPAY_URL_NOTIFICACION="http://localhost:3000/api/pagos/izipay/webhook"

# Cloudinary: completar con las credenciales de la cuenta.
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Correo transaccional: completar al contratar/configurar Resend.
RESEND_API_KEY=""
CORREO_REMITENTE="reservas@tu-dominio-verificado.com"
CORREO_ADMINISTRADOR="administracion@tu-dominio.com"

# WhatsApp Business Cloud API: se utilizará para confirmaciones operativas.
WHATSAPP_TOKEN=""
WHATSAPP_ID_NUMERO_TELEFONO=""
PORT=3500
